{%- macro coil_class(value) -%}
    {%- if value == True -%}
        success
    {%- else -%}
        danger
    {%- endif -%}
{%- endmacro -%}
{%- set coils_active = [] %}
{%- set coils_inactive = [] %}
<!DOCTYPE html>
<html lang="en">

    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
        <meta name="author" content="Eric O'Callaghan">
        <meta name="description" content="Modbus testing.">
        <meta name="keywords" content="Modbus">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta property="og:type" content="website">
        <meta property="og:site_name" content="Modbus">
        <meta property="og:title" content="Modbus">
        <meta property="og:description" content="Modbus testing.">
        <meta property="og:url" content="https://modbus.home.ericoc.com/">
        <title>Modbus</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    </head>

    <body class="card">

        <header class="card-header container-fluid">

            <h1 title="Modbus" class="card-title">
                <a class="card-text" href="{{ url_for('index') }}">Modbus</a>
            </h1>

        </header>

        <main class="card-body">

            <ol class="list-group list-group-horizontal list-group-flush">
{%- for number, value in coils.items() %}
    {%- if value %}
        {%- set _ = coils_active.append(number) -%}
    {%- else %}
        {%- set _ = coils_inactive.append(number) -%}
    {%- endif %}
                <li class="border-0 card-text list-group-item fw-bold" title="Toggle coil #{{ number }} from {{ value }} to {{ not value }}." value="{{ number }}">
                    {{ number }}.
                    <a class="border border-dark btn btn-{{ coil_class(value) }} list-group-item-text m-0 p-0 text-{{ coil_class(value) }}" href="{{ url_for('toggle', coil=number, value=not value) }}" role="button" aria-label="Toggle coil #{{ number }} from {{ value }} to {{ not value }}.">
                        █
                    </a>
                </li>
{%- endfor %}
            </ol>

        </main>

        <footer class="card-footer text-white">

            <p>
                <a class="border border-dark btn btn-secondary card-text" href="{{ url_for('toggle_all') }}" role="button">
                    Toggle All
                </a>
            </p>

            <div class="row">
                <p class="border border-dark col bg-secondary fw-bold m-1 rounded">
                    {{ coils|length }} coils
                </p>
                <p class="border border-dark col bg-success m-1 rounded">
                    <b>{{ coils_active|length }}</b> on
                </p>
                <p class="border border-dark col bg-danger m-1 rounded">
                    <b>{{ coils_inactive|length }}</b> off
                </p>
            </div>

        </footer>

    </body>

</html>
